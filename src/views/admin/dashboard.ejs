<!DOCTYPE html>
<html lang="vi">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Admin Dashboard</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <style>
      body {
        background-color: #f8f9fa;
      }
      .dashboard-card {
        border-radius: 12px;
        box-shadow: 0 4px 8px rgba(0, 0, 0, 0.05);
        transition: 0.3s;
      }
      .dashboard-card:hover {
        box-shadow: 0 6px 16px rgba(0, 0, 0, 0.1);
      }
    </style>
  </head>
  <body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
      <div class="container-fluid">
        <a class="navbar-brand" href="#">Admin Panel</a>
        <div class="d-flex">
          <span class="navbar-text text-white me-3"
            >Xin chào, <%= user.name %></span
          >
          <a href="/logout" class="btn btn-outline-light btn-sm">Đăng xuất</a>
        </div>
      </div>
    </nav>

    <div class="container mt-5">
      <h2 class="mb-4">Dashboard Quản trị</h2>
      <div class="row g-4">
        <div class="col-md-4">
          <div class="card dashboard-card p-4 text-center bg-white">
            <h5 class="card-title mb-3">Quản lý sản phẩm</h5>
            <a href="/admin/products" class="btn btn-primary w-100"
              >Xem chi tiết</a
            >
          </div>
        </div>

        <div class="col-md-4">
          <div class="card dashboard-card p-4 text-center bg-white">
            <h5 class="card-title mb-3">Quản lý đơn hàng</h5>
            <a href="/admin/orders" class="btn btn-primary w-100"
              >Xem chi tiết</a
            >
          </div>
        </div>
        <div class="col-md-4">
          <div class="card dashboard-card p-4 text-center bg-white">
            <h5 class="card-title mb-3">Quản lý danh mục</h5>
            <a href="/admin/categories" class="btn btn-primary w-100"
              >Xem chi tiết</a
            >
          </div>
        </div>

        <div class="col-md-4">
          <div class="card dashboard-card p-4 text-center bg-white">
            <h5 class="card-title mb-3">Quản lý người dùng</h5>
            <a href="/admin/users" class="btn btn-primary w-100"
              >Xem chi tiết</a
            >
          </div>
        </div>
      </div>
    </div>
  </body>
</html>
