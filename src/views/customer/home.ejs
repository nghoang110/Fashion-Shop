<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Fashion-Shop</title>
    <link rel="stylesheet" href="/css/style.css" />
    <link rel="stylesheet" href="/css/slide.css" />
  </head>
  <body>
    <%- include("../partials/header") %>
    <header id="header-slider" class="header-slider-container"></header>
    <div class="container py-5">
      <h2 class="text-center fw-bold mb-4">Danh mục nổi bật</h2>
      <div class="row g-4">
        <% categories.forEach(cat => { %>
        <div class="col-6 col-md-3">
          <a
            href="/products?category=<%= cat._id %>"
            class="text-decoration-none text-dark d-block text-center"
          >
            <div class="category-card rounded shadow-sm overflow-hidden">
              <img
                src="<%= cat.image %>"
                class="w-100"
                style="aspect-ratio: 1/1; object-fit: cover"
                alt="<%= cat.name %>"
              />
            </div>
            <p class="mt-2 fw-semibold"><%= cat.name %></p>
          </a>
        </div>
        <% }) %>
      </div>
    </div>

    <div class="text-center">
      <h1 class="display-5 fw-bold">Sản phẩm mới</h1>
      <p class="lead">Khám phá bộ sưu tập thời trang nam</p>
    </div>

    <div class="row mt-5">
      <% if (products.length === 0) { %>
      <p>Không có sản phẩm nào.</p>
      <% } else { %> <% products.forEach(product => { %>
      <div class="col-md-4 mb-4">
        <div class="card h-100">
          <img
            src="<%= product.image %>"
            class="card-img-top"
            alt="<%= product.name %>"
          />
          <div class="card-body d-flex flex-column">
            <h5 class="card-title"><%= product.name %></h5>
            <p class="card-text"><%= product.description %></p>
            <p class="fw-bold text-danger mb-3">
              <%= product.price.toLocaleString() %> đ
            </p>
            <a
              href="/product/<%= product._id %>"
              class="btn btn-outline-dark mt-auto"
              >Xem chi tiết</a
            >
          </div>
        </div>
      </div>
      <% }); %> <% } %>
    </div>
    <script src="/js/slide.js"></script>
    <script>
      // Khởi tạo header slider khi trang đã tải xong
      document.addEventListener("DOMContentLoaded", function () {
        createHeaderSlider("header-slider");
      });
    </script>

    <%- include("../partials/footer") %>
  </body>
</html>
